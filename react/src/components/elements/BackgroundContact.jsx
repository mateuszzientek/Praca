import React from 'react';

function BackgroundContact({ children }) {
    return (
        <div className="main-background" >
            {children}
        </div>
    );
}

export default BackgroundContact;